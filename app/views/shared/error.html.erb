<%= form_with(model: article, local: true) do |form| %>
  <% if etype.errors.any? %>
    <div id="error_explanation">
      <div class="alert alert-info" role="alert">

        <h2 class="alert-heading"><%= pluralize(etype.errors.count, "error") %> prohibited this article from being saved:</h2>

        <ul>
        <% etype.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>

      </div>
    </div>
  <% end %>